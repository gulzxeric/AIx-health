# AGENTS.md

## 项目定位

RetinaEcho（数字记忆相框）是面向轻中度阿尔茨海默病患者家庭的非临床陪伴与照护支持产品。产品包含患者端全屏 Web、家属端移动 Web/H5、FastAPI 后端和异步 Worker。

任何实现不得将产品描述为医疗器械，不得输出诊断、筛查、患病风险或治疗结论。

## 开始工作前必读

按以下优先级阅读文档：

1. `PRD/数字记忆相框（双端统一）PRD.md`：统一产品需求与验收依据。
2. `spec/2026-09-04-retinaecho-mvp-design.md`：正式技术设计依据。
3. `docs/reference/RetinaEcho-MVP-技术规格说明（中文参考版）.md`：中文阅读参考；与正式 Spec 冲突时以正式 Spec 为准。
4. `PRD/数字记忆相框（患者端）PRD.md`、`PRD/家属端PRD.md`：历史输入，仅用于追溯，不得覆盖统一 PRD 的结论。

开始任务前先执行 `git status --short`，保留用户已有修改，不覆盖或回滚无关变更。

## 不可破坏的产品与安全边界

1. 患者无账号，不承担业务授权或知情同意。
2. `patient_id` 是患者唯一业务身份；不得通过姓名、手机号、照片或声音自动合并患者。
3. 其他家属只能通过管理员邀请加入已有患者空间，不能重新创建“同名患者”。
4. 所有患者级接口必须在服务端校验家属关系或设备归属；前端隐藏按钮不是鉴权。
5. 业务权限、同意、拒绝和撤回全部在家属端完成并审计。
6. 摄像头/麦克风浏览器弹窗是唯一设备侧技术动作，必须由照护者完成；服务端同意撤回后应用立即停止使用硬件。
7. `extraction_confidence` 只用于记忆抽取复核，不得出现在患者端或健康简报中。
8. 低于 `0.600` 的记忆必须由家属明确确认或修改，未经确认不得进入患者对话。
9. 照片与声音通过 `persona_id` 关联，禁止依赖人物显示名匹配。
10. 原始摄像头视频和连续环境音频不得上传。
11. 声音克隆和照片微动需要产品级开关与素材级授权同时有效。
12. 所有模型与媒体能力失败时必须降级，不能使患者端白屏、卡死或失去基础陪伴。

## 架构约束

- 使用患者端和家属端两个 Web 前端、FastAPI 模块化单体、PostgreSQL、Redis、私有对象存储和后台 Worker。
- MVP 不拆微服务。
- 后端按 `auth`、`patients`、`devices`、`consents`、`personas`、`memories`、`chat`、`events`、`summaries`、`media`、`audit` 分模块。
- 模块拥有自己的路由、服务、仓储、Schema 和测试；跨模块写操作走服务接口。
- 前后端契约以 OpenAPI 为准，TypeScript 类型由契约生成，不重复手写漂移类型。
- LLM、ASR、TTS、声音克隆、照片动画和短信供应商必须通过 Adapter 隔离。
- 长耗时 AI/媒体工作通过 Transactional Outbox 和 Worker 执行，不占用同步请求线程。
- 所有任务处理器必须幂等，执行前重新检查权限、同意和资源最新状态。

## 数据与 API 规则

- 持久实体使用 UUIDv7；对外暴露不透明前缀 ID。
- API 时间使用 UTC RFC 3339；本地日历计算使用患者 IANA 时区。
- 创建、绑定、邀请接受和上传启动接口支持 `Idempotency-Key`。
- 可修改资源使用整数 `revision` 做乐观并发，冲突返回 409。
- 列表接口使用不透明游标分页。
- 错误返回稳定机器码；客户端不得解析错误文案。
- 私有媒体只通过短期签名 URL 访问。
- Token、验证码、邀请码、完整手机号、签名 URL、声音样本和对话正文不得进入日志。

## 代码组织原则

- 文件按职责拆分，一个模块只负责一个清晰领域。
- 不为未来假设提前引入抽象；只有存在两个真实实现或明确测试替身时才提取接口。
- 业务规则放在服务层，路由只做协议转换、身份解析和调用编排。
- 数据库查询必须显式包含患者范围，禁止先全局查询再在应用层过滤。
- 前端状态机使用显式事件和 Guard，不用分散布尔值拼接患者端状态。
- 面向患者的错误必须转换为可恢复状态或温和提示，禁止展示堆栈、错误码或医学术语。
- 中英文用户文案集中管理，不在组件中散落硬编码。

## 测试要求

任何功能改动至少覆盖：

- 正常路径。
- 权限拒绝路径。
- 同意缺失或撤回路径。
- 重试/幂等路径。
- Provider 失败后的降级路径。
- 患者隔离：两个患者的数据绝不能交叉。

鉴权、绑定、邀请、同意、记忆确认、患者删除必须有数据库集成测试。接口变更必须更新 OpenAPI 和契约测试。修复 Bug 时先增加能复现问题的失败测试。

完成任务前运行与改动相关的最小测试集，再运行所属应用的完整测试集。项目尚未建立可执行命令时，不得虚构测试通过；应明确记录未运行原因。

## 文档维护

- 产品行为变化先更新统一 PRD，再更新正式 Spec，最后更新中文参考版和实现。
- 纯技术实现变化只需更新正式 Spec；若影响中文读者理解，同步更新参考版。
- 不直接修改两份历史单端 PRD，除非用户明确要求。
- 文档中不得留下 `TODO`、`TBD`、占位符或互相冲突的阈值。

## Git 协作

- 提交前检查 `git diff --check` 和 `git status --short`。
- 一个提交只包含一个可解释的变更主题。
- 不提交密钥、Token、真实手机号、患者数据或私有媒体。
- 不使用破坏性命令回滚用户工作，不修改无关文件。
- 分支默认使用 `codex/` 前缀；用户指定分支名时服从用户要求。
- 提交信息使用英文 Conventional Commit，例如 `feat: add patient binding flow`、`docs: update consent design`。

## 完成标准

任务只有在以下条件全部满足时才算完成：

1. 行为符合统一 PRD 和正式 Spec。
2. 患者隔离、角色权限和同意门槛在服务端生效。
3. 正常、拒绝、失败和降级路径均有测试或可复现验证。
4. OpenAPI、迁移、前端状态和文档与实现一致。
5. `git diff --check` 通过，工作区只包含本任务预期变更。
